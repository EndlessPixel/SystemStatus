{
    "note": {
        "1": "前端获取信息选项 - 多服务器分支配置",
        "2": "branches对象中可添加多个服务器配置",
        "3": "default_branch指定默认选中的服务器分支key",
        "4": "每个分支包含name(显示名)、api(服务器地址)、port(端口，可选，不填则使用默认端口)",
        "5": "示例：添加反向代理服务器时可省略port字段"
    },
    "default_branch": "local_server",
    "branches": {
        "local_server": {
            "name": "本地服务器",
            "api": "127.0.0.1",
            "port": 8001
        },
        "reverse_proxy_example": {
            "name": "反向代理示例",
            "api": "example.com",
            "comment": "此配置不指定端口，将使用默认80/443端口"
        }
    }
}
